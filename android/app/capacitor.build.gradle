// DO NOT EDIT — managed by Capacitor (npx cap sync regenerates this).
// Applies plugin-specific build configuration for each Capacitor plugin.

android {
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
